torch>=2.0
numpy>=1.24
scikit-learn>=1.3
sentence-transformers>=2.2

# Required only by tools/extract_layer_embeddings.py (audio feature extraction).
# Not needed if you already have precomputed layer statistics.
transformers>=4.38
librosa>=0.10
tqdm>=4.65
